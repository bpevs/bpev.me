{
  "importMap": "./import_map.json",
  "fmt": {
    "options": {
      "proseWrap": "never",
      "singleQuote": true,
      "semiColons": false
    }
  },
  "tasks": {
    "start": "deno run --allow-read  --allow-net --allow-run --allow-write=/var --allow-env=ESBUILD_BINARY_PATH,HOME --watch main.ts"
  }
}
